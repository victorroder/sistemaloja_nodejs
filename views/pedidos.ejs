<%- include ('partials/header.ejs') %>
<%- include ('partials/navbar.ejs') %>

<div class="container my-4">
    <div class="card shadow-sm">
        <div class="card-header">
            <h1>Pedidos</h1>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th></th>
                        <th>Número:</th>
                        <th>Valor:</th>
                    </tr>
                </thead>
                <tbody>
                    <% pedidos.forEach(pedido => { %>
                    <tr>
                        <td><img src="imgs/order.png" class="img-thumbnail" width="30" alt="Pedido"></td>
                        <td><%= pedido.numero %></td>
                        <td><%= pedido.valor.toLocaleString('pt-br', {style: 'currency', currency: 'BRL'}) %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <div class="total">
                <h4>Total: <%= total.toLocaleString('pt-br', {style: 'currency', currency: 'BRL'}) %></h4>
            </div>
        </div>
    </div>
</div>

<%- include ('partials/footer.ejs') %>
